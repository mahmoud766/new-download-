# دليل الربط والتهيئة والرفع على استضافة هوستنجر (Hostinger MySQL Web Installer)

تمت تهيئة وتجهيز هذا التطبيق بشكل كامل وتخليصه من أي قواعد بيانات خارجية، ليعتمد مباشرة وبشكل تام على **قواعد بيانات هوستنجر نفسها (Hostinger MySQL / MariaDB)** مع نظام تثبيت تفاعلي (Web Installer) مثل قوالب كود كانيون (CodeCanyon).

---

## 🛠️ المميزات والتهيئة التي تم إعدادها تلقائياً

1. **معالج تثبيت قواعد البيانات التفاعلي (CodeCanyon-Style Web Installer)**:
   - تم إنشاء نافذة تثبيت تفاعلية تفتح بنقرة زر واحدة من الهيدر أو لوحة التحكم باسم **"قاعدة بيانات Hostinger"**.
   - تتيح لك إدخال:
     * **مضيف قاعدة البيانات (Host)**: افتراضياً `localhost`.
     * **اسم قاعدة البيانات (Database Name)**: مثلاً `u123456789_downloader`.
     * **اسم مستخدم القاعدة (DB Username)**: مثلاً `u123456789_user`.
     * **كلمة المرور (DB Password)**.
     * **المنفذ (Port)**: `3306`.
   - عند الضغط على **"اختبار الاتصال وتثبيت قاعدة البيانات"**:
     * يتم اختبار الاتصال بقاعدة بيانات Hostinger MySQL.
     * يتم إنشاء وتثبيت جميع الجداول المطلوبة تلقائياً (`trending_downloads`, `site_settings`, `download_logs`, `visitor_stats`).
     * يتم حفظ التكوين تلقائياً.

2. **واجهات برمجة التطبيقات الذاتية (PHP API Layer)**:
   - تتواجد مجلدات الـ API في `public/api/` وتتضمن:
     * `public/api/install.php`: معالج التثبيت والربط التلقائي.
     * `public/api/db.php`: موصل PDO الخاص بـ Hostinger MySQL.
     * `public/api/trending.php`: حفظ واسترجاع التنزيلات الأكثر شيوعاً حياً من MySQL.
     * `public/api/settings.php`: حفظ إعدادات الموقع والإعلانات والمدونة في MySQL.
     * `public/api/logs.php`: تسجيل واسترجاع سجلات التحميل في MySQL.
     * `public/api/analytics.php`: تسجيل عدد الزوار اليومي في MySQL.

3. **ملف التوجيه المتقدم والأمان (`.htaccess`)**:
   - يعالج توجيه صفحات التطبيق (SPA Routing) لضمان عدم ظهور خطأ **404 Not Found**.
   - تحويل تلقائي إلى اتصال **HTTPS**.
   - تفعيل ضغط **GZIP** وتأمين الهيدرز.

---

## 🚀 خطوات الرفع وتثبيت القاعدة على هوستنجر (خطوة بخطوة)

### الخطوة 1: إنشاء قاعدة البيانات في هوستنجر (Hostinger hPanel)
1. افتح لوحة تحكم هوستنجر **hPanel**.
2. اذهب إلى قسم **قواعد البيانات (Databases)** -> **MySQL Databases**.
3. قم بإنشاء قاعدة بيانات جديدة، واكتب:
   - **Database Name** (اسم قاعدة البيانات)
   - **Database Username** (اسم المستخدم)
   - **Password** (كلمة المرور)
4. احفظ هذه البيانات جانباً.

---

### الخطوة 2: بناء ملفات الإنتاج (Production Build)
1. قم بتشغيل الأمر التالي لبناء الكود النهائي:
   ```bash
   npm run build
   ```
2. سينتج مجلد **`dist`** المحتوي على كود الـ React وملفات الـ PHP في `dist/api/`.

---

### الخطوة 3: الرفع إلى المجلد الرئيسي الجذر (`public_html`)
1. في هوستنجر **File Manager**، افحص مجلد **`public_html`**.
2. ارفع جميع الملفات والمجلدات الموجودة **داخل مجلد `dist`** مباشرةً إلى داخل `public_html`.
3. تأكد من وجود `index.html` وملف `.htaccess` ومجلد `api/` في الجذر الرئيسي لـ `public_html`.

---

### الخطوة 4: تشغيل التثبيت التفاعلي (Web Installer)
1. افتح موقعك من المتصفح (مثال: `https://yourdomain.com`).
2. اضغط على زر **"قاعدة بيانات Hostinger"** في الشريط العلوي (أو من لوحة التحكم).
3. أدخل اسم قاعدة البيانات واسم المستخدم وكلمة المرور التي أنشأتها في الخطوة 1.
4. اضغط على **"اختبار الاتصال وتثبيت قاعدة البيانات الآن"**.

---

## 🟢 النتيجة
سيتم ربط موقعك فوراً بقاعدة بيانات هوستنجر وتثبيت كافة الجداول تلقائياً، ويعمل الموقع بسرعة فائقة وبدون الاعتماد على أي خدمات خارجية!
