# دليل التهيأة والرفع الشامل على استضافة هوستنجر (Hostinger Shared Hosting)

تمت تهيئة وتجهيز هذا التطبيق بشكل كامل ليعمل على **استضافة هوستنجر المشتركة (Hostinger Shared Hosting)** في مجلد الجذر (`public_html`).

---

## 🛠️ التجهيزات والتحسينات التي تم إعدادها تلقائياً للموقع

1. **ملف التوجيه والأمان المتقدم (`.htaccess`)**:
   - يتواجد في مجلد `public/.htaccess` وسيتم تضمينه تلقائياً داخل مجلد `dist/` عند البناء.
   - يعالج توجيه صفحات التطبيق (Single Page Application - SPA Routing) لضمان عدم ظهور خطأ **404 Not Found** عند تنشيط أو تحديث أي صفحة داخلية.
   - يحول جميع الزوار تلقائياً إلى الاتصال الآمن **HTTPS**.
   - يفعل ضغط المخرجات **GZIP** لتسريع فتح الموقع.
   - يضيف هيدرز الأمان لحماية الموقع من التجسس والاختراق.

2. **ربط قاعدة البيانات وتخزين البيانات (Firebase Firestore & Auth)**:
   - التطبيق يتصل مباشرة عبر واجهات برمجية آمنة (Client-Side SDK) بقاعدة بيانات **Firestore** والمعرف المسجل للمشروع (`ai-studio-omnidownloader-c89b452c-bc94-46eb-8075-9f67751722d7`).
   - يعمل الربط مع قاعدة البيانات بشكل ممتاز وسريع جداً على الاستضافة المشتركة دون الحاجة لخادم Node.js معقد أو إعدادات خوادم إضافية.

---

## 🚀 خطوات الرفع والنشر على هوستنجر (خطوة بخطوة)

### الخطوة 1: بناء ملفات الإنتاج (Production Build)
1. قم بتشغيل الأمر التالي على جهازك لبناء الكود النهائي:
   ```bash
   npm run build
   ```
2. سينتج عن هذا الأمر مجلد جديد باسم **`dist`** يحتوي على جميع ملفات الموقع الجاهزة للنشر (بما فيها `index.html` وملف `.htaccess`).

---

### الخطوة 2: الدخول إلى لوحة هوستنجر (Hostinger hPanel)
1. افتح لوحة تحكم هوستنجر **hPanel**.
2. انتقل إلى قسم **المواقع (Websites)** واختر موقعك الإلكتروني.
3. اضغط على **مدير الملفات (File Manager)**.

---

### الخطوة 3: الرفع إلى المجلد الرئيسي الجذر (`public_html`)
1. داخل **File Manager**، افحص مجلد **`public_html`**.
2. إذا كان يحتوي على ملف افتراضي مثل `default.php` أو `index.php` تابع لهوستنجر، قم بحذفه.
3. قم بضغط محتويات مجلد **`dist`** كملف ZIP أو ارفع جميع الملفات والمجلدات الموجودة **داخل مجلد `dist`** مباشرةً إلى داخل `public_html`.
   *(ملاحظة مهمة: ارفع **محتويات** مجلد `dist` وليس المجلد نفسه حتى تظهر الصفحة الرئيسية فور الدخول على نطاق موقعك)*.
4. تأكد من استخراج الملفات بحيث يكون `index.html` و `.htaccess` في الجذر الرئيسي لـ `public_html`.

---

### الخطوة 4: التأكد من إظهار الملفات المخفية (`.htaccess`)
- في مدير ملفات هوستنجر، تأكد من تفعيل خيار **"Show Hidden Files"** لتأكيد وجود ملف `.htaccess`.
- هذا الملف ضروري جداً لضمان عمل كافة أزرار وصفحات الموقع بدون خطأ 404.

---

### الخطوة 5: تفعيل شهادة الأمان SSL
1. من لوحة هوستنجر hPanel، اذهب إلى قسم **الأمان (Security)** -> **SSL**.
2. تأكد من تفعيل شهادة SSL المجانية (Free SSL) الخاصة بموقعك.

---

## 🟢 النتيجة والتحقق
بمجرد إتمام الرفع، قم بزيارة رابط موقعك على هوستنجر:
- ستعمل كافة المحولات واقسام التحميل، لوحة التحكم، وقاعدة بيانات Firebase المباشرة بسرعة فائقة وبدون أي مشاكل!
